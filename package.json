{
  "name": "simplyauth-web",
  "private": true,
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@noble/ciphers": "2.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
